# Copy this file to ".env" and fill in real values.
# ".env" is git-ignored and must never be committed.
#
# NOTE: a Resend API key must only be used from a server / serverless
# backend, never from client-side static pages (it would be public).
RESEND_API_KEY=re_your_key_here
